# Handoff

## Active goal
Package A (`calculus-foundations`) is **approved** (repository owner,
2026-07-31), including a narrow E2E waiver. Active work has moved to three
things approved together: (1) the Gate 9 assessment plan for
`calculus-foundations`, now specified; (2) implementing that Gate 9 assessment
in code; (3) Mode B docs and Mode C lesson code for L5 `chain-rule`, Package
B's first lesson.

## Scope
In scope: Gate 9 code implementation (fixtures, module items, manifest,
grading contracts, module sets, two supporting code changes to `cueLint` and
`ConstructCheck`), L5's four Mode B docs through an actual Gate 4 result, L5
lesson code if Gate 4 reaches `PASS`, full verification, and a review pass
over the new work. Out of scope: any other package, the L1–L3 stale-checkbox
sweep, and Package B's `implementation-package.md` (only written once all of
Package B's lessons are planned).

**Model-routing note.** AGENTS.md routes Mode B insight-contract authoring and
new-capability design to Opus. This work is proceeding on Sonnet 5 per the
user's explicit instruction after switching the session model; artifacts
authored under this deviation carry an inline note saying so, rather than
silently reading as Opus-authored.

## Verified state
See the module ledger's own §6/§7
(`docs/courses/applied-mathematics/modules/calculus-foundations/implementation-package.md`)
for Package A's approval record — do not duplicate it here.

## Canonical references
- Package ledger and acceptance checklist:
  `docs/courses/applied-mathematics/modules/calculus-foundations/implementation-package.md`
  §6–§8
- Gate 9 assessment plan:
  `docs/courses/applied-mathematics/modules/calculus-foundations/assessment-plan.md`
- L5 Mode B artifacts: `docs/courses/applied-mathematics/lessons/05-chain-rule/`
- Approved plan for this work: `/home/thomas/.claude/plans/velvety-spinning-clarke.md`
- Task routing: `AGENTS.md`

## Branch / worktree
`master`, no worktrees in play. `CLAUDE.md` still has a pre-existing
uncommitted "Session continuation" diff from an earlier session (origin
unknown) — still undecided: commit, keep editing, or discard.

## Test results
In progress this session — see the ledger and assessment plan for what has
actually been run once this pass completes. Do not trust this section over
those.

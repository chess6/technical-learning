# Module assessment plan (Gate 9)

Fill-in template for a **module's cumulative assessment**. This is the Gate 9
artifact that certifies *integration and delayed retention across a module* and
**discharges the module-owned outcomes** its lessons deferred.

> **Saved as:** `docs/courses/<course>/modules/<module>/assessment-plan.md`
> (module id = the `curriculum.ts` section id, e.g. `systems-elimination`).

Follow [authoring/mastery-standard.md §6](../mastery-standard.md#6-assessment-architecture)
(assessment architecture) and draw items from
[authoring/assessment-patterns.md](../assessment-patterns.md). Reference the lesson
mastery contracts; do not restate them.

## Module metadata
- **Module / section id:** `<e.g. systems-elimination>`
- **Lessons in module:** `<L3 systems, L4 elimination, L5 solution-sets>`
- **Profile target for this module:** `<P2 primary | P3 override | …>` (from
  [course-spine §0](../../<course>/course-spine.md) — link the real path).

## Module-owned outcomes to discharge here
> Pulled from each lesson's mastery contract (its **module-owned** rows). Each must
> be independently demonstrated with **real** results before it is claimed mastered.

| Outcome (operational) | From lesson | Dimension | Target level | Assessment item | Result |
| --- | --- | --- | --- | --- | --- |
| `<e.g. Combine elimination + solution-set structure on a mixed system>` | L4/L5 | D10 | E5 | `<item id>` | `<pass/…>` |

## Abstraction-return deferrals discharged here
> Any accountable abstraction-return whose **owner is this module**
> ([semantic-page-grammar §4.3](../../product/semantic-page-grammar.md#4-mathematical-object-and-representation-standard)):
> the destination reached and the item that shows the learner *operating* there.

| Deferred from | Destination (general case) | Item | Result |
| --- | --- | --- | --- |
| `<lesson id>` | `<e.g. ℝⁿ null space>` | `<item id>` | `<…>` |

## The assessment set (cumulative · interleaved · spaced)
- **Cumulative problems (D10):** mix ≥2 lessons in one problem.
- **Method-selection (D8):** problems that do **not** name the method.
- **Interleaved review:** mixed problem types so the learner must choose.
- **Delayed / spaced retrieval (D12):** which earlier outcomes resurface, and when.
- **Timed / exam-mode set (D11):** if the profile claims S3 — deferred feedback.
- **Error-diagnosis (D13):** ≥1 "which step + why" item.

## Results & readiness
- **Module-owned outcomes discharged:** `<n / N>` with real evidence.
- **Delayed-retention verified:** `<yes/no + when>`.
- **Readiness claim supported (per [COURSE §6.2](../mastery-standard.md#62-what-evidence-justifies-each-readiness-claim)):**
  `<"module mastered" | "exam ready" | "proof-ready" | none yet>` — cite the
  evidence layer + level; never claim beyond what the results show.

Feeds the course-level [benchmark validation](module-validation.md) / Gate 10.

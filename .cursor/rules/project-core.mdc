---
description: Core architecture, scope, and conventions for the linear algebra visual learning POC
alwaysApply: true
---

# Project core

Desktop-first Vite/React/TS POC. Prefer the existing layering; do not invent parallel math or lesson pipelines.

## Layers (keep boundaries)

| Layer | Path | Rule |
| --- | --- | --- |
| Pure math | `src/math/` | No React, no Mafs, no Motion Canvas. Production must not import `mathjs` (`mathjs` is devDependency only). |
| Shared examples | `src/math/examples.ts`, `src/lessons/exampleData.ts` | Guided + explorers share example ids — never duplicate matrix/vector constants. |
| Guided | `src/guided-scenes/` | Lessons pass `guidedSceneId` only; React never imports Motion Canvas scene modules directly. |
| Explorations | `src/explorations/` | Register in `registry.tsx`; reuse `MafsSceneShell`, controls, toggles, readouts. |
| Lessons / UI | `src/lessons/`, `src/components/lesson/` | Registry-driven; no per-lesson branching in the shell. |

## Scope

- **Built surface** (in `src/lessons/curriculum.ts` as `kind: "lesson"`):
  `why-linear-algebra`, `vectors`, `transformations`, `systems`, `elimination`,
  `solution-sets`, `determinants`, `eigenvectors` (intro), plus the `karatsuba`
  track. These are the active product.
- Spine nodes marked `kind: "future"` are **closed to implementation**: do not
  write lesson code for, or promote (`future → built`), a `future` lesson unless
  the user **explicitly reopens that specific lesson**. This is a hard approval
  boundary (Mode C). *Docs-only Mode B **planning*** (insight brief → insight
  contract → mastery contract → plan, Gates 3–5) **may** proceed for a uniquely
  resolved next node — see the workflow's short-prompt rule — but must stop before
  any code or promotion.
- Prefer fixing visual/math correctness and shared helpers over new lesson content.
- **Any course or lesson authoring request** — architecture, planning, or
  implementation — must be routed through
  [docs/authoring/course-authoring-workflow.md](../../docs/authoring/course-authoring-workflow.md) first
  (see the `course-authoring` rule).

## Math & copy

- Learner-facing math uses KaTeX (`ProseWithMath`, equation/readout components) — including MC choices when they are matrices/vectors.
- Prefer `matrixVectorMultiply` / shared helpers over ad-hoc formulas in UI code.

## Correctness docs

Before changing math or visualizations: `docs/engineering/math-correctness.md`, `docs/quality/known-failure-modes.md`. Checklist: `docs/quality/lesson-correctness-checklist.md`.

## Verify

After meaningful changes: `npm run lint`, `npm run test`, and e2e when touching player/layout/lesson flow (`npm run test:e2e`).

## Browser review screenshots

Save app screenshots **only** under `screenshots/` in the project workspace (gitignored). Always pass an absolute path such as `/home/thomas/Dev/technical-learning/screenshots/<name>.png` to Playwright / browser tools. Never write review captures to the home directory, Downloads, or other unrelated locations.
